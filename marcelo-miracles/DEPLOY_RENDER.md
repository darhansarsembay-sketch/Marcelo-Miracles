# Деплой на Render.com - Инструкция

## Проблема
Render ищет файлы в `/opt/render/project/src/backend`, но структура проекта другая.

## Решение

### Вариант 1: Настройка через веб-интерфейс Render (Рекомендуется)

1. Зайдите на [render.com](https://render.com) и создайте новый **Web Service**
2. Подключите ваш GitHub репозиторий
3. **ВАЖНО**: В настройках сервиса укажите:
   - **Root Directory**: `backend` (не `/backend`, просто `backend`)
   - **Environment**: `Node`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`

4. Добавьте переменные окружения:
   - `BOT_TOKEN` = ваш токен бота
   - `SUPABASE_URL` = ваш Supabase URL
   - `SUPABASE_KEY` = ваш Supabase ключ
   - `ADMIN_IDS` = (опционально, через запятую)
   - `PORT` = Render автоматически установит, но можно оставить пустым

5. Нажмите **Create Web Service**

### Вариант 2: Использование render.yaml (Автоматический)

Если вы используете render.yaml файл (который я создал), Render может автоматически определить настройки, но лучше использовать Вариант 1 для первого раза.

## После деплоя

1. Render даст вам URL вида: `https://your-app.onrender.com`
2. Обновите `VITE_BACKEND_URL` во фронтенде на этот URL
3. Пересоберите и задеплойте фронтенд

## Проверка

После деплоя проверьте:
- `https://your-app.onrender.com/api/orders` (должен вернуть ошибку, но не 404)
- Логи в Render Dashboard должны показывать: "🚀 Marcelo Miracles Backend running on port..."

## Примечания

- Render бесплатный план может "засыпать" после 15 минут неактивности
- Первый запрос после сна может занять 30-60 секунд
- Для production лучше использовать платный план или другой хостинг

